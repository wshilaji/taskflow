syntax = "proto3";

package ecm.taskflow;
option cc_enable_arenas = true;

message NodeProto {
  string name = 1;
  string op = 2;

  map<string, string> attrs = 3;
  repeated string input = 4;
}

message GraphProto {
  string name = 1;
  repeated NodeProto node = 2;
}